#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../tsup/dist/cli-default.js" "$@"
else
  exec node  "$basedir/../../../../tsup/dist/cli-default.js" "$@"
fi
# cmd-shim-target=C:/Users/paul_/Documents/Claude/jwt-attacker/node_modules/.pnpm/@caido-community+dev@0.1.7_postcss@8.5.15_typescript@5.9.3/node_modules/tsup/dist/cli-default.js
